/*#include "Globals.hpp"

class Jugador
{
    public:
        Jugador(sf::String nombre, sf::Color color, unsigned short int id);
        virtual ~Jugador();

        //Setters
        void setId(unsigned short int id)           { Jugador::id     = id;     }
        void setNombre(sf::String nombre)           { Jugador::nombre = nombre; }
        void setPuntos(unsigned short int puntos)   { Jugador::puntos = puntos; }
        void setColor(sf::Color color)              { Jugador::color  = color;  }

        //Getters
        unsigned short int getId()      {  return Jugador::id;      }
        sf::String getNombre()          {  return Jugador::nombre;  }
        unsigned short int getPuntos()  {  return Jugador::puntos;  }
        sf::Color getColor()            {  return Jugador::color;   }
        bool isActivo()                 {  return Jugador::activo;  }

        //Otras utiles / Others utils
        void activar(bool activo) { Jugador::activo = activo; }

    protected:

    private:
        char id;
        sf::String nombre;
        unsigned short int puntos;
        bool activo;
        sf::Color color;
};*/
