/*#include "Ventana.hpp"

Ventana::Ventana(float ancho, float alto, int pBits, std::string titulo, sf::Color colorFondo, bool dimensionable)
{
    if(dimensionable)
        Ventana::ventana = new sf::RenderWindow(ancho, alto, pBits, titulo);
    else
        Ventana::ventana = new sf::RenderWindow(ancho, alto, pBits, titulo);

    Ventana::ancho      = ancho;
    Ventana::alto       = alto;
    Ventana::pBits      = pBits;
    Ventana::colorFondo = colorFondo;
}

Ventana::~Ventana()
{
    /dtor
}*/
